# LCD Virtual-SD Browser

Planned Klipper extension providing hierarchical, refresh-on-entry navigation
of directories below `[virtual_sdcard] path` on a character LCD.

The plugin is scaffolded here to reserve its stable installation name. No
runtime payload is present yet, so the repository installer will report it as
planned and skip it until its `klippy/extras` or `config` payload is added.

