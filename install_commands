git clone https://github.com/DonutCatDev/Formative_SV08_Plugins.git
cd Formative_SV08_Plugins
./install-plugins.sh

# Noninteractive alternatives:
# ./install-plugins.sh --list
# ./install-plugins.sh lcd_vsd_browser
# ./install-plugins.sh --all

# The LCD browser is activated automatically through an installer-owned link in
# ~/printer_data/config/options/lcd/ and the existing *.cfg wildcard include.

# Remove one plugin later with:
# ./install-plugins.sh --uninstall lcd_vsd_browser
