git clone https://github.com/DonutCatDev/Formative_SV08_Plugins.git
cd Formative_SV08_Plugins
./install-plugins.sh

# Noninteractive alternatives:
# ./install-plugins.sh --list
# ./install-plugins.sh lcd_vsd_browser
# ./install-plugins.sh network_status
# ./install-plugins.sh --all

# Plugin configuration is activated through installer-owned entrypoints under
# ~/printer_data/config/custom_plugins/*.cfg.

# Remove one plugin later with:
# ./install-plugins.sh --uninstall lcd_vsd_browser
# ./install-plugins.sh --uninstall network_status

# For machines with the old ~/klipper_network_status checkout, open the
# interactive manager and choose network_status -> Remove legacy installation
# before installing the replacement.
